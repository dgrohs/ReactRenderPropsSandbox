import React from "react";
//import { ThemeContext } from "./../context/ThemeContext";

// use the Consumer API
// to style the component

export default Component => {
  return props => (
    // TODO code something
    <Component {...props} TODO code something />
    // TODO code something
  );
};
